<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.zucc.music.dao.FollowMapper" >
  <resultMap id="BaseResultMap" type="cn.edu.zucc.music.model.Follow" >
    <id column="follow_id" property="followId" jdbcType="INTEGER" />
    <result column="from_user_id" property="fromUserId" jdbcType="VARCHAR" />
    <result column="to_user_id" property="toUserId" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    follow_id, from_user_id, to_user_id
  </sql>

  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from Follow
    where follow_id = #{followId,jdbcType=INTEGER}
  </select>

  <select id="getFollowedUsers" resultMap="BaseResultMap" parameterType="java.lang.String">
    SELECT *
    FROM Follow
    WHERE from_user_id = #{fromUserId, jdbcType=VARCHAR}
  </select>

  <select id="getFansUsers" resultMap="BaseResultMap" parameterType="java.lang.String">
    SELECT *
    FROM Follow
    WHERE to_user_id = #{toUserId, jdbcType=VARCHAR}
  </select>

  <select id="getFromMutual" resultMap="BaseResultMap" parameterType="java.lang.String">
    SELECT *
    FROM Follow
    WHERE from_user_id = #{fromUserId, jdbcType=VARCHAR} and to_user_id = #{toUserId, jdbcType=VARCHAR}
  </select>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from Follow
    where follow_id = #{followId,jdbcType=INTEGER}
  </delete>

  <select id="getMaxFollowId" resultType="java.lang.Integer">
    SELECT count(*)
    FROM Follow
  </select>

  <insert id="insert" parameterType="cn.edu.zucc.music.model.Follow" >
    insert into Follow (follow_id, from_user_id, to_user_id
      )
    values (#{followId,jdbcType=INTEGER}, #{fromUserId,jdbcType=VARCHAR}, #{toUserId,jdbcType=VARCHAR}
      )
  </insert>



  <insert id="insertSelective" parameterType="cn.edu.zucc.music.model.Follow" >
    insert into Follow
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="followId != null" >
        follow_id,
      </if>
      <if test="fromUserId != null" >
        from_user_id,
      </if>
      <if test="toUserId != null" >
        to_user_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="followId != null" >
        #{followId,jdbcType=INTEGER},
      </if>
      <if test="fromUserId != null" >
        #{fromUserId,jdbcType=VARCHAR},
      </if>
      <if test="toUserId != null" >
        #{toUserId,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKeySelective" parameterType="cn.edu.zucc.music.model.Follow" >
    update Follow
    <set >
      <if test="fromUserId != null" >
        from_user_id = #{fromUserId,jdbcType=VARCHAR},
      </if>
      <if test="toUserId != null" >
        to_user_id = #{toUserId,jdbcType=VARCHAR},
      </if>
    </set>
    where follow_id = #{followId,jdbcType=INTEGER}
  </update>

  <update id="updateByPrimaryKey" parameterType="cn.edu.zucc.music.model.Follow" >
    update Follow
    set from_user_id = #{fromUserId,jdbcType=VARCHAR},
      to_user_id = #{toUserId,jdbcType=VARCHAR}
    where follow_id = #{followId,jdbcType=INTEGER}
  </update>


</mapper>